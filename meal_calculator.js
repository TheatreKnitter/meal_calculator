// JavaScript File
var diner = {
    name: "",
    var dishes = {
        dish: [ ],
        price: [ ],
    },
    var cost = 0;
    for(var i=0; i<this.dishes.price.length; i++)
    cost += myArray[i],
    tax: this.cost*0.08875,
    tip: this.cost*0.2,
};

var bill = {
    list: [ ],
    totalTax: this.cost + this.tax,
    totalTip: this.cost + this.tip,
    print: [this.name, this.cost, this.tax, this.tip],
}

var Tom = object.create(diner);
Tom.name = "Tom";
Tom.dishes.dish = ["beans", "rice"];
Tom.dishes.price = [2.25, 2];

var Joan = object.create(diner);
Joan.name = "Joan";
Joan.dishes.dish = ["spinach", "salmon"];
Joan.dishes.cost = [4.50, 10.00];

bill.list.push('Tom', 'Joan');


