// JavaScript File
var diner = {
    name: "",
    var dishes = {
        dish: [ ],
        price: [ ],
    },
    var cost = 0;
    for(var i=0; i<diner.dishes.price.length; i++)
    cost += myArray[i],
    tax: diner.cost*0.08875,
    tip: diner.cost*0.2,
};

var bill = {
    list: diners,
    totalTax: diner.cost + diner.tax,
    totalTip: diner.cost + diner.tip,
    print: [diner.name, diner.cost, diner.tax, diner.tip],
}